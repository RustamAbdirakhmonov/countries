import 'package:flutter/material.dart';
class CitiesScreen extends StatelessWidget {
  const CitiesScreen({Key? key}) : super(key: key);
  static const routeArgs="/cities";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
