import 'package:flutter/material.dart';
class QuizScreen extends StatelessWidget {
  const QuizScreen({Key? key}) : super(key: key);
  static const routeArgs="/quiz";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
